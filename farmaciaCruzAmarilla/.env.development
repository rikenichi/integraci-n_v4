# En desarrollo, axios usa /api y Vite hace proxy al backend MEDISTOCK (ver vite.config.js).
# En producción, reemplaza por la URL pública del backend en AWS, p.ej:
#   VITE_MEDISTOCK_API=https://api.medistock.cl/api
VITE_MEDISTOCK_API=/api
