# Use /api during Vite development so requests go through vite.config.js proxy.
VITE_API_URL=/api

# Direct backend access also remains supported when needed:
# VITE_API_URL=http://localhost:8000/api
